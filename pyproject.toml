[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "millionsend"
version = "0.1.0"
description = "Official Python SDK for MillionSend — a self-hostable, Resend-compatible email API."
readme = "README.md"
requires-python = ">=3.9"
license = { text = "MIT" }
authors = [{ name = "MillionSend" }]
keywords = ["millionsend", "email", "resend", "ses", "transactional"]
dependencies = ["requests>=2.25"]

[project.urls]
Homepage = "https://github.com/MillionSend/millionsend"
Repository = "https://github.com/MillionSend/millionsend"

[project.optional-dependencies]
dev = ["pytest>=7"]

[tool.hatch.build.targets.wheel]
packages = ["millionsend"]

[tool.pytest.ini_options]
testpaths = ["tests"]
